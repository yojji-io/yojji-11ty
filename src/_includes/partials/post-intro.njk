<header class="l-container l-measure">
  <div class="l-post-intro">
    <img src="{{cover}}" style="width: 100%;"/>
    <h1>{{ title }}</h1>
    {% if description %}
      <div>{{ description | safe }}</div>
    {% endif %}
  </div>
</header>
